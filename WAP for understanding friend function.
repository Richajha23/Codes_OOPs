#include<iostream>
using namespace std;
class demo
{
int a,b;
public:
void getdata()
{
  cout<<"enter the value of a and b";
  cin>>a>>b;};
  void add(demo obj1)
  {
    cout<<a<<b<<obj1.a<<obj1.b;
    cout<<"\n sum of two number is"<<obj1.a+obj1.b;
  }};
int main()
{
  demo d1;
  d1.getdata();
  d1.add(d1);
  return 0;
}
